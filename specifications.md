# Project Specifications

- Rebuild the quiz interface using **React** with **Tailwind CSS** for styling.
- Keep submit buttons hidden until the user clicks **Start Quiz**.
- Maintain these requirements going forward unless updated.
